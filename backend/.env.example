# BioLearn Backend Environment Variables

# Secret key for JWT tokens — generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production

# Database URL (SQLite default, or use PostgreSQL)
DATABASE_URL=sqlite+aiosqlite:///./biolearn.db

# CORS origins (comma-separated). With Nginx proxy this can stay empty.
CORS_ORIGINS=

# Template directory (absolute path, default: ../template relative to backend)
TEMPLATE_DIR=
